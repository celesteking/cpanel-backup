source "http://rubygems.org"

# Specify your gem's dependencies in cpanelbackup.gemspec
gemspec

if ENV['host'] == 'yu'
	gem 'cpanel-helper', '~> 0.1.0', :path => '/storage/personal/work/coding/cpanelhelper'
else
	gem 'cpanel-helper', '~> 0.1.0'
end
